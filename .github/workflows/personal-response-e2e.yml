name: Personal response browser regression

on:
  pull_request:
    paths:
      - '.github/workflows/personal-response-e2e.yml'
      - 'package.json'
      - 'package-lock.json'
      - 'playwright.config.ts'
      - 'playwright.personal-response.config.ts'
      - 'src/app/accessibility-release.css'
      - 'src/app/personal-response-release.css'
      - 'src/components/personal-invitation/**'
      - 'src/modules/invitation-templates/**'
      - 'tests/e2e/**'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/personal-response-e2e.yml'
      - 'package.json'
      - 'package-lock.json'
      - 'playwright.config.ts'
      - 'playwright.personal-response.config.ts'
      - 'src/app/accessibility-release.css'
      - 'src/app/personal-response-release.css'
      - 'src/components/personal-invitation/**'
      - 'src/modules/invitation-templates/**'
      - 'tests/e2e/**'

permissions:
  contents: read

jobs:
  browser-regression:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check J1R-E formatting
        run: |
          npx prettier --check \
            .github/workflows/personal-response-e2e.yml \
            package.json \
            playwright.config.ts \
            playwright.personal-response.config.ts \
            src/modules/invitation-templates/invitation-template-renderer.tsx \
            tests/e2e

      - name: Capture repository lint
        run: npm run lint > j1r-e-lint.txt 2>&1 || true

      - name: Upload lint evidence
        uses: actions/upload-artifact@v4
        with:
          name: j1r-e-lint-evidence
          path: j1r-e-lint.txt
          retention-days: 1

      - name: Typecheck repository
        run: npm run typecheck

      - name: Run unit tests
        run: npm test

      - name: Build production application
        run: npm run build

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run personal response browser regression
        run: npm run e2e:personal-response

      - name: Upload Playwright evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: personal-response-playwright-report
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
          retention-days: 7
