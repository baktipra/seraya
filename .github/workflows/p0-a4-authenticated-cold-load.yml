name: P0-A4 authenticated cold-load evidence

on:
  pull_request:
    branches: [main]

permissions:
  actions: read
  contents: read

jobs:
  measure:
    if: github.head_ref == 'agent/p0-a4-readiness-aggregation-recovery'
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ github.token }}
      P0_A4_BASE_URL: https://seraya-8yuuqsl8d-bakti-s-projects.vercel.app/?_vercel_share=lcpZs7jYZmnkrieJhMIdEQkYIVmdW5w3
      P0_A4_MEASURED_SHA: 3659ae46f51dd7484089170846d19a51e5fea304
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Capture authenticated cold loads
        run: node scripts/measure-p0-a4-cold-loads.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: p0-a4-authenticated-cold-load-evidence
          path: p0-a4-cold-load-evidence
          if-no-files-found: warn
          retention-days: 1
